/**
 * Indexer stub (Phase 0).
 *
 * Non-scope:
 * - RPC providers
 * - persistence layers
 * - data pipelines
 *
 * This file exists only to anchor the layer.
 */
export function indexerStub(): void {
  // Intentionally empty.
}
