/**
 * Map stub (Phase 0).
 *
 * Non-scope:
 * - rendering engines
 * - UI frameworks
 * - chain access
 *
 * This file exists only to anchor the layer.
 */
export function mapStub(): void {
  // Intentionally empty.
}
